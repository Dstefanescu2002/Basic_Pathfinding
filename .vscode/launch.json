{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/probability_grid.exe",
            "args": [],
            "stdio": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}